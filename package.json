{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build --censor-lib --strict",
    "test": "pulp test",
    "perf": "pulp test --test-path perf --main Perf.Main"
  },
  "devDependencies": {
    "pulp": "^9.0.1",
    "purescript-psa": "^0.3.9",
    "rimraf": "^2.5.0"
  }
}
